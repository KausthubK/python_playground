# Numerical Programming Fluency Tests

## Env

- pytest picks up all tests but Test Explorer compatibility to show parametrised tests or tests nested under classes.
- need to run `poetry run pytest .` or `poetry run pytest tests/test_problemN.py -v` in terminal for things to work.

## Problem 1