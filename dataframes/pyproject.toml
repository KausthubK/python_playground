[tool.poetry]
name = "dataframes"
version = "0.1.0"
description = ""
authors = ["Kausthub Krishnamurthy <kausthub.k@live.com>"]

[tool.poetry.dependencies]
python = "^3.10"
pandas = "^2.3.3"
polars = "^1.35.2"
pyarrow = "^22.0.0"
ibis-framework = {extras = ["datafusion"], version = "^11.0.0"}
duckdb = "^1.4.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
