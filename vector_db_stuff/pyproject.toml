[project]
name = "vector_db_stuff"
version = "0.1.0"
description = ""
authors = [
    {name = "kausthub.k@live.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "chromadb (>=1.3.5,<2.0.0)"
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[virtualenvs]
in-project = true

[tool.poetry]
package-mode = false
